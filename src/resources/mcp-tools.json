[
  {
    "name": "prepare_handover",
    "description": "Prepare a session handover. Returns a /pickup command that the user can send in any IM channel connected to VibeAround to resume the session there.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "session_id": {
          "type": "string",
          "description": "Your current session ID. If omitted, VibeAround will try to find the most recent session for the given cwd."
        },
        "cwd": {
          "type": "string",
          "description": "Absolute path to your current working directory / project workspace."
        },
        "agent_kind": {
          "type": "string",
          "description": "Your agent type (e.g. claude, gemini, codex, cursor, kiro, qwen-code)."
        }
      },
      "required": ["cwd", "agent_kind"]
    }
  },
  {
    "name": "register_workspace",
    "description": "Register a project workspace directory in VibeAround. Required before handover if the workspace is not already registered.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "cwd": {
          "type": "string",
          "description": "Absolute path to the project workspace directory to register."
        },
        "name": {
          "type": "string",
          "description": "Optional human-readable name for the workspace."
        }
      },
      "required": ["cwd"]
    }
  },
  {
    "name": "preview",
    "description": "Start a live preview for a running local server. Returns owner and share URLs. Pass session_id (use /va-session skill) for proper lifecycle management.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "port": {
          "type": "number",
          "description": "The localhost port where the dev server is running."
        },
        "cwd": {
          "type": "string",
          "description": "Absolute path to the project workspace directory."
        },
        "session_id": {
          "type": "string",
          "description": "Your agent session ID (use /va-session skill to resolve). Enables automatic dev-server cleanup when the session ends."
        },
        "title": {
          "type": "string",
          "description": "Optional title shown in the preview toolbar. Defaults to the workspace directory name."
        }
      },
      "required": ["port", "cwd"]
    }
  },
  {
    "name": "md_preview",
    "description": "Preview a markdown file with beautiful GitHub-style rendering. Returns a shareable URL. The URL expires in 5 minutes.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "file": {
          "type": "string",
          "description": "Path to the markdown file (absolute, or relative to cwd)."
        },
        "cwd": {
          "type": "string",
          "description": "Absolute path to the project workspace directory."
        },
        "title": {
          "type": "string",
          "description": "Optional title shown in the preview toolbar. Defaults to the filename."
        }
      },
      "required": ["file", "cwd"]
    }
  }
]
